workflows:
  ios-testflight:
    name: StrikeScore TestFlight Deployment
    max_build_duration: 60
    instance_type: mac_mini_m1
    
    integrations:
      app_store_connect: appleapi

    environment:
      groups:
        - Strikescore
      vars:
        XCODE_WORKSPACE: "StrikeScore.xcworkspace" 
        XCODE_SCHEME: "StrikeScore"
      xcode: 26.0
      cocoapods: default

    triggering:
      events:
        - push
      branch_patterns:
        - pattern: 'main'
          include: true

    scripts:
      - name: Generate Xcode Project and Install Pods
        script: |
          echo "Installing XcodeGen via Homebrew..."
          brew install xcodegen
          
          echo "Generating Xcode project file..."
          xcodegen generate
          
          echo "Installing CocoaPods dependencies..."
          pod install
          
      - name: Initialize keychain and add signing certificates
        script: |
          keychain initialize
          
          # Decode certificate from base64
          echo "$CERTIFICATE_P12_BASE64" | base64 --decode > /tmp/certificate.p12
          keychain add-certificates --certificate /tmp/certificate.p12 --certificate-password "$CERTIFICATE_PASSWORD"
          
      - name: Set up Provisioning Profiles on Xcode project
        script: |
          # Decode provisioning profile from base64
          echo "$PROVISIONING_PROFILE_BASE64" | base64 --decode > /tmp/StrikeScore_App_Store_Profile.mobileprovision
          
          # Create the directory if it doesn't exist
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          
          # Copy profile to where Xcode expects it
          cp /tmp/StrikeScore_App_Store_Profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/
          
          # Apply to project
          xcode-project use-profiles
      
      - name: Set build number
        script: |
          agvtool new-version -all 124
          
      - name: Build iOS IPA
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME" \
            --config Release

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        auth: integration 
        submit_to_testflight: true
