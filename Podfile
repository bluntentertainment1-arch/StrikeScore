platform :ios, '16.0'

target 'StrikeScore' do
  use_frameworks!

  pod 'Google-Mobile-Ads-SDK'
  pod 'Firebase/Core'      # Adds FirebaseCore + FirebaseAnalytics
  # OR for more granular control:
  # pod 'Firebase/CoreOnly'  # Just FirebaseCore, no Analytics
end
